---
const { className = '', icon, label, ...rest } = Astro.props;
---

<div
    {...rest}
    class={`flex items-center gap-1 font-extrabold rounded-lg shadow-black-small border-2 border-black px-2 py-1 ${className}`}
>
    <slot />
</div>
